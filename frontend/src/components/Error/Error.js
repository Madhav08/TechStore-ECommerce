import React from 'react';
import { ErrorContainer, ErrorText } from './Error.elements';

const Error = () => {
  return (
    <ErrorContainer>
      <ErrorText>Sorry, there was some problem please try again !</ErrorText>
    </ErrorContainer>
  );
};

export default Error;
